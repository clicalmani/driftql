import { Elegant } from "driftql-react";

export class $model extends Elegant {
  
  protected static resourceName = '$model';

  /**
   * You can add types for autocomplete.
   */
  declare id: number | undefined;
  
  constructor(data: any) {
    super();
    Object.assign(this, data);
  }
}
